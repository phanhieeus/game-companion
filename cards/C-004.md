# C-004 — Nhập điểm bằng tay

status: done
deps: C-001

## Scope

Đường lui khi giọng nói không dùng được (micro bị chặn, hết quota — cả hai đã
xảy ra thật). Nút "Nhập tay" mở form: mỗi người chơi một ô số, hiện tổng đang
cộng được, nút Ghi chỉ bật khi ván hợp lệ.

Đi qua đúng `record_round` với `source: "manual"` (ADR quyết định 4) — dùng chung
validate zero-sum, idempotency, undo, và hiện trong bảng y như ván nói bằng giọng.
KHÔNG tạo đường ghi điểm thứ hai.

Không sửa được ván cũ ở card này — chỉ thêm ván mới. Sửa từng ô đã cắt ở stage 04.

## Allowed files

- `src/ui/ManualEntry.tsx` (mới)
- `src/ui/App.tsx`
- `src/ui/styles.css`
- `e2e/manual.spec.ts` (mới)

## Verify (run these before calling the card done)

- [x] E2E: ghi một ván hoàn toàn bằng tay, KHÔNG chạm tới SpeechRecognition giả
      → bảng và bảng tổng đều cập nhật
- [x] E2E: tổng ≠ 0 → nút Ghi bị vô hiệu hoá và hiện lý do; không ghi gì
- [x] E2E: ván nhập tay hiện trong bảng giống hệt ván nói bằng giọng
- [x] Đếm số lần chạm từ lúc bấm "Nhập tay" tới khi bảng cập nhật ≤ 4
      (ngoài việc gõ số) — success metric #4 của PRD
- [x] `npx tsc -b` sạch

## Done-evidence (world-state proof, named BEFORE building)

Ảnh `screenshots/8-manual.png` chụp form đang mở với số đã gõ và tổng hiện đúng,
kèm ảnh sau khi ghi cho thấy ván xuất hiện trong bảng. Kèm output test chứng minh
đường ghi điểm không dùng giọng nói chạy được end-to-end.

## Evidence (paste the actual proof here when done)

**Ảnh:** `screenshots/8-manual.png` — form nhập tay đang mở, đã gõ
Nam 3 / Hùng −1 / Lan −1 / Tú −1 / Minh trống, hiện "Tổng 0 ✓", nút Ghi bật.

```
$ npx playwright test e2e/manual.spec.ts
  ✓ 1 ghi một ván hoàn toàn bằng tay, không dùng giọng nói (687ms)
  ✓ 2 tổng khác 0 thì nút Ghi bị khoá và nói rõ lý do (508ms)
  ✓ 3 ván nhập tay hủy được y như ván nói bằng giọng (590ms)
  ✓ 4 PRD metric #4: ≤ 4 thao tác chạm ngoài việc gõ số (531ms)
  4 passed (4.3s)
```

**Điểm mấu chốt của bộ test này:** không test nào chạm tới `SpeechRecognition`
giả, và không test nào mock `/api/interpret`. Chúng xanh nghĩa là đường ghi điểm
không-dùng-giọng-nói chạy độc lập thật — đúng thứ card này tồn tại để bảo đảm.
Nếu mai quota hết hoặc micro bị chặn, vẫn ghi điểm được.

Test 2 chứng minh validate dùng chung: thông báo "Tổng điểm của ván phải bằng 0"
đến từ chính `validateRoundEntries` của domain, không phải chuỗi viết lại trong
UI — tức nhập tay đi qua đúng luật như ván nói bằng giọng (ADR quyết định 4).

Test 4 đếm được đúng **2** thao tác chạm điều hướng (mở form, bấm Ghi), dưới
ngưỡng 4 của PRD.

**Sửa trong lúc làm:** ảnh đầu tiên cho thấy nhãn "Nhập tay" bị xuống dòng thành
hai dòng do grid chật — assertion không bắt được, nhìn ảnh mới thấy. Đã thêm
`white-space: nowrap`.

`npx tsc -b` sạch.
