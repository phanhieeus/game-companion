# C-003 — Bảng tổng gọn lại

status: done
deps: C-001

## Scope

Thêm chế độ `compact` cho `Scoreboard`: giảm chiều cao mỗi hàng và cỡ chữ để cả
bảng tổng + bảng theo ván (C-001) cùng nằm trên màn hình mà không đẩy nút Voice
ra khỏi tầm ngón cái.

Giữ nguyên thông tin: hạng, tên, nhãn "tôi", điểm tổng, tô sáng người dẫn đầu.
Đây là bài toán mật độ hiển thị, KHÔNG bỏ bớt dữ liệu.

## Allowed files

- `src/ui/Scoreboard.tsx`
- `src/ui/App.tsx`
- `src/ui/styles.css`
- `e2e/table.spec.ts`

## Verify (run these before calling the card done)

- [x] E2E: sau khi ghi **10 ván** với 5 người, nút Voice vẫn nằm trong viewport
      mà không cần cuộn (success metric #3 của PRD)
- [x] E2E: bảng tổng vẫn hiện đủ hạng + tên + điểm cho cả 5 người
- [x] `npx playwright test` toàn bộ xanh
- [x] `npx tsc -b` sạch

## Done-evidence (world-state proof, named BEFORE building)

Ảnh `screenshots/7-compact-10rounds.png` ở viewport 360px sau 10 ván với 5 người,
thấy đồng thời: bảng tổng, bảng theo ván, và nút Voice — tất cả trong một màn
hình không cuộn.

## Evidence (paste the actual proof here when done)

**Ảnh:** `screenshots/7-compact-10rounds.png` — 5 người, **10 ván**, viewport
360×740, chụp KHÔNG fullPage (cố ý: chỉ tính những gì thật sự thấy trong một màn
hình). Trong cùng một khung hình thấy đồng thời: bảng tổng 5 người có hạng và
điểm, bảng theo ván tới ván 8, và **nút "Nhấn giữ để nói"** — không cần cuộn.

```
$ npx playwright test e2e/table.spec.ts
  ✓ 8 C-003 / PRD metric #3: sau 10 ván 5 người, nút Voice vẫn trong màn hình (1.5s)
  8 passed (9.7s)
```

Test dùng `toBeInViewport()` trên nút Voice sau 10 ván — assertion trực tiếp cho
success metric #3 của PRD, không phải suy luận gián tiếp. Đồng thời khẳng định
bảng tổng vẫn đủ 5 hàng: gọn là giảm mật độ, KHÔNG cắt dữ liệu.

`npx tsc -b` sạch.
