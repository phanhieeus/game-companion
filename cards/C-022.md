# C-022 — Ghi vết agent: chuỗi ReAct, prompt, guardrail

status: done
deps: C-021

## Scope

Đang ở giai đoạn phát triển, mà agent là hộp đen: nói một câu, ván hiện ra, không
ai biết nó nghĩ gì ở giữa. Card này mở hộp — **chỉ quan sát, không đổi hành vi**.

Mỗi lượt nói ghi lại:

1. Câu người dùng nói, `deviceId`, phiên, thời điểm.
2. **System prompt đầy đủ** đã gửi đi (nó đổi theo roster và trí nhớ, nên không
   suy ngược được — phải lưu thật).
3. **Từng bước ReAct**: model trả chữ gì (thought), gọi tool nào với tham số gì
   (action), tool trả về gì (observation). Đúng thứ tự, kèm thời gian mỗi bước.
4. **Sự kiện guardrail** từ C-021: chặn nhịp gọi, quá dài, delta phi lý, tên bị
   lọc, chạm trần ván. Cộng các chốt đã có: cần xác nhận (HITL), chặn zero-sum,
   chạm trần 5 bước, tool lạ, lỗi Gemini kèm `retryable`.
5. Kết cục lượt đó: final / confirm / clarify / error, tổng số bước, tổng thời gian.

Lưu vào bảng `agent_traces`. **Giữ 200 lượt gần nhất mỗi phiên** — đủ soi trọn
một buổi chơi, mà CSDL free không phình tới lúc đứt.

KHÔNG thuộc card này: giao diện xem (C-023).

## Allowed files

- `api/tracing.py` (mới), `api/routes/agent.py`, `api/agent/loop.py`, `api/agent/types.py`
- `api/main.py` (chọn kho theo môi trường)
- `api/repository/traces.py` (mới), `tests/test_tracing.py` (mới)
- `flow/05-contract.md`

## Verify (run these before calling the card done)

- [x] Unit: một lượt hai bước → trace có đúng 2 bước, đúng thứ tự thought→action→obs
- [x] Unit: observation trong trace KHỚP kết quả tool thật (không phải tóm tắt)
- [x] Unit: lượt bị chốt HITL → trace ghi sự kiện confirm và tool CHƯA chạy
- [x] Unit: chạm trần 5 bước → trace ghi đủ 5 bước + sự kiện guardrail
- [x] Unit: lỗi Gemini → trace giữ nguyên `retryable`
- [x] Unit: quá 200 lượt → lượt cũ nhất bị dọn, lượt mới còn nguyên
- [x] Unit: ghi trace hỏng KHÔNG làm hỏng lượt nói (quan sát không được phép
      làm gãy thứ nó quan sát)
- [x] `pytest` xanh, e2e cũ vẫn xanh

## Done-evidence (world-state proof, named BEFORE building)

Trên production: nói một câu ghi ván thật, rồi đọc trace của phiên đó ra và thấy
đủ chuỗi — prompt đã gửi, model gọi `record_round` với tham số gì, chốt HITL
chặn lại ở đâu, sau khi đồng ý thì tool trả về gì. Dán nguyên chuỗi đó vào đây.

## Evidence (paste the actual proof here when done)

**Ba lượt nói thật với Gemini THẬT**, đọc thẳng từ vết đã ghi:

```
■ 'Nam ăn 3, ba người kia mỗi người chung 1'  →  confirm (909ms)
   ⚑ hitl_confirm_required: {"tool":"record_round","prompt":"Nam +3, Hùng -1, …"}
   bước 0: [prompt 1463 ký tự] ACTION record_round({"entries":[{"delta":3,"player":"Nam"}…)
           (không có OBSERV — tool CHƯA chạy, đúng như chốt HITL phải làm)

■ '[chốt: đồng ý] record_round'  →  final (1195ms)
   bước 0: ACTION record_round({"entries":[{"delta":3,"player":"Nam"}…)
           OBSERV {"recorded":true,"round":1,"scoreboard":{…}}
   bước 1: [prompt 1463 ký tự] THOUGHT 'Đã ghi xong ván 1: Nam được 3, Hùng, Lan và Tú…'

■ 'ai đang dẫn điểm?'  →  final (1798ms)
   bước 0: [prompt 1463 ký tự] ACTION get_scoreboard({})
           OBSERV [{"name":"Nam","total":3,"rank":1},{"name":"Hùng","total":-1,…}]
   bước 1: [prompt 1463 ký tự] THOUGHT 'Nam đang dẫn đầu với 3 điểm.'
```

Đọc được đúng ba thứ operator cần: **prompt** đã gửi (1463 ký tự, bản thật),
**function calling** kèm tham số, và **observation** là kết quả thật của tool.
Lượt tra cứu hiện rõ vòng ReAct hai bước.

```
$ .venv/bin/python -m pytest   → 165 passed, 8 skipped
$ npx playwright test          → 48 passed
```

### Ba lỗi bị bắt, mỗi lỗi một kiểu

1. **Prompt lệch một nhịp.** `begin_step(prompt)` chạy TRƯỚC khi prompt được
   dựng, nên bước 0 luôn rỗng còn bước 1 mang prompt của bước trước. Đọc log sẽ
   hiểu sai agent đã thấy gì — loại lỗi tệ nhất cho công cụ quan sát: nó không
   im lặng, nó nói dối. Tách thành `set_prompt()` gọi ngay trước lượt gọi model.
   Test đơn vị bắt.
2. **Đường chốt sập.** `resume_agent` chạy tool TRƯỚC khi vào vòng lặp, lúc đó
   chưa có bước nào → `TypeError`, cả lượt chốt hỏng. **E2E bắt, pytest không**
   — vì test đơn vị của tôi luôn gọi đúng thứ tự. Giờ cả ba hàm ghi đều tự mở
   bước nếu chưa có, và có test cho từng hàm gọi lệch thứ tự.
3. **`record_round(null)` ở lượt chốt.** Ghi tên tool mà không ghi tham số —
   người đọc log không biết đã ghi CÁI GÌ. Nhìn output thật mới thấy; không
   assertion nào bắt. Giờ `tool_ran` nhận `args`.

### Chưa chứng minh được: PgTraceStore trên production

Bằng chứng trên là kho FILE chạy local. Bản Postgres **chưa được đọc lại lần
nào** — mà thiết kế "nuốt mọi lỗi khi ghi vết" khiến một kho hỏng là hoàn toàn
im lặng: agent vẫn chạy, log vẫn trống, không ai biết.

Chưa đọc được vì C-022 chỉ GHI; đường đọc là C-023. **Verify đầu tiên của C-023
phải là: mở admin trên production và thấy đúng lượt vừa nói.** Không thấy thì
lỗi nằm ở card này, không nằm ở C-023.

### Ghi chú thiết kế

- **Tracer là tuỳ chọn trong `ToolContext`** — không có thì agent chạy y hệt.
  Nhờ vậy 14 test vòng lặp không phải dựng thêm gì, và tracing không thể đổi
  hành vi agent.
- **Dọn theo TỪNG PHIÊN** (200 lượt), không theo tổng: một phiên nói nhiều
  không được phép xoá mất vết của phiên khác. Có test riêng cho điều đó.
- **Số liệu đếm lại từ chính vết đã lưu**, không nuôi bộ đếm riêng — bộ đếm
  riêng là nguồn sự thật thứ hai và nó sẽ lệch.
- **Lượt bị guardrail chặn cũng để lại vết** (`outcome: "blocked"`, `steps: []`).
  Không có nó thì người dùng kêu "app không nhận" mà admin nhìn log thấy trống.
