# C-024 — UI bong bóng chat, nhập bằng chữ hoặc giọng nói

status: done
deps: C-023

## Scope

**Bảng điểm giữ NGUYÊN** — operator nói rõ, và nó đã qua bốn card mài giũa
(C-001, C-003, C-005, C-007). Chỉ đổi khu tương tác.

1. Khu dưới bảng thành **khung hội thoại**: bong bóng người dùng và bong bóng
   trợ lý, cuộn được, giữ lại lượt trước chứ không chỉ hiện câu cuối.
2. **Composer**: ô nhập chữ + nút gửi + nút micro. Gửi bằng chữ đi CÙNG endpoint
   `/agent` — không thêm đường thứ hai vào agent.
3. **Nút micro giữ kiểu nhấn-giữ-để-nói** (operator chọn): nhả tay là dừng, không
   bao giờ nghe lén.
4. Thẻ đề xuất thành một bong bóng có các dòng số + nút Ghi / Bỏ qua, nằm trong
   mạch hội thoại thay vì nổi đè lên đáy màn hình.
5. Trạng thái "đang nghĩ" hiện trong khung chat.

**23 chỗ trong e2e** đang bám vào nút "Nhấn giữ để nói" và `.proposal`. Đổi
selector, GIỮ NGUYÊN mọi assertion về hành vi — chúng là lưới an toàn, không phải
thứ dọn cho tiện.

## Allowed files

- `src/ui/Chat.tsx` (mới), `src/ui/Composer.tsx` (mới), `src/ui/App.tsx`
- `src/ui/ProposalCard.tsx`, `src/ui/styles.css`, `src/ui/tokens.css`
- `src/conversation/useConversation.ts`, `e2e/*.spec.ts`

## Verify (run these before calling the card done)

- [ ] Gõ chữ rồi bấm gửi → agent trả lời, ván vào bảng như khi nói
- [ ] Nhấn giữ micro rồi nhả → vẫn hoạt động như cũ
- [ ] Thẻ đề xuất hiện đủ dòng số trong bong bóng, bấm Ghi thì ván vào bảng
- [ ] Hội thoại giữ lại lượt trước, cuộn được
- [ ] @360px: bảng điểm KHÔNG cuộn ngang, composer không che bảng (metric PRD #2, #3)
- [ ] Bàn phím ảo mở lên không đẩy composer ra khỏi màn hình
- [ ] Cả 44 e2e xanh sau khi đổi selector, không xoá assertion nào
- [ ] `npm run build` sạch

## Done-evidence (world-state proof, named BEFORE building)

Ảnh `screenshots/16-chat.png` @360px trên production: một mạch hội thoại thật có
ít nhất hai lượt (một gõ chữ, một nói), một bong bóng đề xuất kèm số, và bảng
điểm phía trên vẫn nguyên dạng cũ. Kèm output 44 e2e xanh.

## Evidence (paste the actual proof here when done)

### `screenshots/16-chat.png` — production, 360×740, Gemini thật

Chụp trên https://ghi-diem.onrender.com sau deploy `dep-d9rlspajobas73donrm0`
(live 23:52). Chỉ SpeechRecognition bị giả — nó là API trình duyệt, không phải
thứ card này làm. Server, Gemini, Postgres đều thật.

Trạng thái DOM đọc ra cùng lúc chụp:

```json
{
  "bubbles": [
    "bubble you: Nam ăn ba, ba người kia mỗi người chung một",
    "bubble agent: Đã ghi xong ván 1, Nam được 3 điểm, Hùng, Lan và Tú mỗi người trừ 1 điểm.",
    "bubble you: Ai đang dẫn?",
    "bubble agent: Nam đang dẫn đầu với 3 điểm.",
    "bubble you: Lan ăn hai, Nam với Hùng mỗi người chung một"
  ],
  "proposal": ["Lan +2", "Tú 0", "Nam -1", "Hùng -1"],
  "rows": 1,
  "overflowX": 0,
  "chatScrolls": true,
  "ghiInViewport": true
}
```

Đủ những gì done-evidence đòi: **hai lượt khác đường vào** — "Ai đang dẫn?" là
GÕ CHỮ, hai câu ghi ván là NÓI — nằm chung một mạch; **thẻ đề xuất có đủ bốn
dòng số** hiện trong bong bóng; **bảng điểm phía trên nguyên dạng cũ** (ván 1:
Nam +3, ba người kia −1, `rows: 1` chứng minh lượt nói đầu đã chốt và vào sổ).
`overflowX: 0` — không cuộn ngang @360px.

### Test

```
246 passed, 8 skipped        pytest
16 passed                    vitest
59 passed (1.5m)             playwright
✓ built in 1.02s             npm run build
```

44 → 59 e2e: không xoá assertion nào. 23 mỏ neo cũ còn nguyên vì nút micro giữ
đúng nhãn "Nhấn giữ để nói" (chuyển sang `aria-label`). Chỉ `.agent` phải đổi
thành `.bubble.agent:not(.thinking)` — vì giờ trên màn hình có NHIỀU lượt, một
selector trỏ vào "câu agent" không còn đơn nghĩa nữa.

### Ba lỗi chỉ bằng chứng thật mới lộ ra

**1. Nút Ghi bị đẩy khỏi màn hình.** Đưa thẻ đề xuất vào mạch chat (đúng như
scope đòi) làm nó cuộn theo trang; chơi 16 ván xong thì phải cuộn xuống mới chốt
được ván. E2E `table.spec.ts:148` đỏ ngay — Playwright phải tự cuộn để bấm được
Ghi, tức người dùng cũng vậy. C-003 (metric PRD #3) đã chốt điều ngược lại. Sửa:
neo cả khối chat + thanh nhập xuống đáy màn hình.

**2. Thẻ đề xuất bị cắt mất người ăn.** Ảnh production LẦN ĐẦU cho thấy khung
cuộn xuống đáy thì mép trên thẻ bị cắt: dòng "Lan +2" — đúng con số lớn nhất —
nằm ngoài màn hình, người dùng bấm Ghi mà chưa từng nhìn thấy nó. Đây là T của
T·C·R hỏng đúng chỗ nó sinh ra để bảo vệ, và 57 e2e đang xanh không cái nào bắt
được. Sửa hai tầng: `.chat:has(.ask)` nới trần khi đang chờ chốt; và khi thẻ vẫn
cao hơn khung (màn ngắn) thì neo mép TRÊN — cắt từ dưới thì cuộn một cái là tới
nút Ghi, cắt từ trên thì số biến mất mà chẳng báo gì.

**3. Ô Σ đè lên khung chat.** Cột "Ván" dính trái (C-025) mang z-index riêng nên
trồi lên trên khối neo đáy. Thuần thị giác, chỉ ảnh chụp mới thấy.

Hai e2e mới canh lỗi #2 đã được KIỂM NGƯỢC: tắt phần sửa đi để chắc chúng thật
sự đỏ. Lần thử đầu chúng vẫn xanh — vì `toBeInViewport` mặc định chỉ cần chạm
mép là đạt, tức là nó xanh cả khi thẻ bị cắt mất 40%. Phải thêm `ratio: 1`. Một
test canh "nhìn thấy" mà không nói rõ nhìn thấy BAO NHIÊU thì không canh gì cả.

### Chưa chứng minh được

- **Bàn phím ảo thật.** Verify "bàn phím ảo mở lên không đẩy composer ra khỏi
  màn hình" chỉ được kiểm GIÁN TIẾP bằng viewport 360×480 (≈ màn hình còn lại
  khi bàn phím chiếm chỗ). Đó không phải bàn phím thật: `visualViewport` co lại
  theo cách khác, và Safari iOS còn khác Chrome Android. Ô nhập đã để đúng 16px
  để chặn cái hỏng đã biết (iOS tự phóng to cả trang), nhưng phần còn lại phải
  thử tay trên máy thật mới biết.
- **Khối neo đáy che hàng Σ lúc đang chờ chốt.** Nới trần khung chat lên 70vh
  khi có thẻ đề xuất nghĩa là hàng tổng ở chân bảng bị che trong lúc đó — cuộn
  lên vẫn thấy, nhưng không liếc được cùng lúc với thẻ. Đánh đổi có chủ ý: lúc
  đang quyết định, thứ cần đọc là các dòng số trong thẻ. Nếu dùng thật thấy
  vướng thì đó là một card mới, không phải sửa lén ở đây.

### Việc còn lại, không thuộc card này

- Model đọc "1" trong câu ngắn thành 100000/300000 (ghi ở C-023). Guardrail delta
  che được, nhưng gốc là chuyện hiểu số.
- Chuỗi lỗi nội bộ rò ra UI (`Gemini trả lỗi 400.`, `Không gọi được Gemini.`,
  `Thiếu GEMINI_API_KEY.`) — đã báo cáo, operator chủ động hoãn xử lý. Giờ chúng
  hiện thành bong bóng `.bubble.agent.error` viền đỏ, tức là chỗ sửa đã gọn lại
  vào một class duy nhất.
