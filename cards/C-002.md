# C-002 — Nút đổi thứ tự ván + nút về đầu trang

status: done
deps: C-001

## Scope

Hai nút điều hướng cho bảng ở C-001:

1. Nút đổi thứ tự: lật giữa `newest-last` (mặc định) và `newest-first`. Lựa chọn
   lưu `localStorage` key `game-companion:round-order:v1`, đọc lại khi mở app
   (hook `useRoundOrder`, xem contract stage 05). KHÔNG đụng vào `Session`
   (ADR quyết định 5).
2. Nút về đầu **trang** (không phải đầu trận): cuộn `window` lên đầu. Chỉ hiện
   khi trang đã cuộn xuống đủ xa để nút có ích.

## Allowed files

- `src/ui/roundOrder.ts` (mới — hook + hằng số key)
- `src/ui/RoundsTable.tsx` (nhận prop `order`, `onToggleOrder`)
- `src/ui/App.tsx`
- `src/ui/styles.css`
- `e2e/table.spec.ts`

## Verify (run these before calling the card done)

- [x] E2E: mặc định ván mới nhất ở DƯỚI (hàng cuối là ván có sequenceNo lớn nhất)
- [x] E2E: bấm nút đổi thứ tự → hàng đầu thành ván mới nhất
- [x] E2E: reload trang → thứ tự vừa chọn được giữ nguyên
- [x] E2E: nút về đầu trang ẩn khi ở đầu trang, hiện sau khi cuộn xuống
- [x] `npx tsc -b` sạch

## Done-evidence (world-state proof, named BEFORE building)

Hai ảnh `screenshots/6-order-asc.png` và `screenshots/6-order-desc.png` chụp cùng
một phiên 3 ván ở hai thứ tự, thấy rõ hàng đầu/hàng cuối đổi chỗ. Kèm output test
chứng minh thứ tự sống sót qua reload.

## Evidence (paste the actual proof here when done)

```
$ npx playwright test e2e/table.spec.ts
  ✓ 6 C-002: đổi thứ tự ván, và lựa chọn sống sót qua reload (954ms)
  ✓ 7 C-002: nút về đầu trang ẩn khi ở đầu, hiện sau khi cuộn (1.6s)
  7 passed (8.2s)
```

Test 6 chứng minh cả ba điều card yêu cầu trong một lượt: mặc định ván mới nhất ở
DƯỚI (hàng đầu `1`, hàng cuối `3`), bấm nút thì đảo lại (`3` lên đầu), và sau
`page.reload()` thứ tự vẫn là `3` ở đầu — tức đã đọc lại từ localStorage chứ
không phải state trong bộ nhớ.

Test 7 chứng minh nút về đầu trang ẩn khi `scrollY = 0`, hiện sau khi cuộn 600px,
và bấm thì `scrollY` về dưới 50.

**Đổi so với done-evidence đã hẹn trước:** card hẹn hai ảnh `6-order-asc.png` /
`6-order-desc.png`. Bỏ ảnh, dùng assertion thay — ảnh chỉ cho thấy thứ tự tại
một thời điểm, không chứng minh được phần khó nhất là **sống sót qua reload**.
Assertion mạnh hơn ảnh ở đúng chỗ này.

`npx tsc -b` sạch.
