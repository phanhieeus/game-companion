# C-020 — Trả lời bằng chữ, chưa đọc thành tiếng

status: done
deps: C-019

## Scope

Operator đã đo trên điện thoại thật: **nhận** giọng nói tiếng Việt đạt, nhưng
giọng **đọc ra** còn tệ. Nghe một câu máy đọc sai ngữ điệu giữa ván bài thì khó
chịu hơn là đọc bằng mắt.

1. Bỏ lời gọi `speak()` (đúng một chỗ trong `useConversation.ts`).
2. **Giữ nguyên đầu vào bằng giọng nói**, gồm cả trả lời "ừ/không" bằng miệng —
   phần đó đã đo là dùng được.
3. Không xoá `src/voice/speech.ts`: đây là tắt, không phải bỏ. Ghi ADR để lần sau
   ai đó thấy hàm `speak` không dùng thì biết vì sao nó còn nằm đó.
4. 3 chỗ e2e đang assert `spokenLines` → đổi sang kiểm chữ hiện trên màn hình.

## Allowed files

- `src/conversation/useConversation.ts`, `src/voice/speech.ts` (chỉ ghi chú)
- `e2e/scoring.spec.ts`, `e2e/fakeSpeech.ts`
- `flow/04-adr.md`

## Verify (run these before calling the card done)

- [x] E2E: nói một câu → câu trả lời HIỆN trên màn hình
- [x] E2E: không còn assertion nào dựa vào `spokenLines`
- [x] E2E: nói "ừ" vẫn chốt được ván (đầu vào giọng nói còn nguyên)
- [x] `grep -rn "speak(" src/` chỉ còn định nghĩa, không còn chỗ gọi
- [x] `playwright` xanh

## Done-evidence (world-state proof, named BEFORE building)

Ảnh chụp app trên production sau một lượt nói: câu trả lời của trợ lý nằm trên
màn hình dưới dạng chữ. Kèm `grep` cho thấy không còn chỗ nào gọi `speak()`.

## Evidence (paste the actual proof here when done)

**Trên production** (deploy `dep-d9rhl37avr4c739aafag`, commit `e96c288`), Gemini
THẬT, chỉ giả `SpeechRecognition` của trình duyệt vì Playwright không có STT.
Nói "Nam ăn 3, ba người kia mỗi người chung 1" → chốt → đọc thẳng từ DOM:

```
TRÊN MÀN HÌNH: "Đã ghi xong ván 1, Nam được 3 điểm, Hùng, Lan và Tú mỗi người trừ 1 điểm."
ĐÃ ĐỌC THÀNH TIẾNG: []
```

Ảnh: `screenshots/15-c020-prod.png` — câu trả lời nằm dưới bảng dạng chữ, kèm
câu vừa nói ở trên nó; bảng ghi +3 / −1 / −1 / −1, Σ khớp.

`grep -rn "speak(" src/` chỉ còn hai dòng, cả hai đều trong `src/voice/speech.ts`:
dòng khai báo `export function speak` và dòng `window.speechSynthesis.speak`
bên trong chính nó. Không còn chỗ nào GỌI nó.

**Cục bộ:** `playwright` 48 passed, `tsc --noEmit` sạch.

Thêm một test canh chừng (`trợ lý trả lời bằng chữ, không đọc thành tiếng`):
đây là chỗ duy nhất còn dùng `spokenLines`, và nó khẳng định log giọng đọc phải
RỖNG sau một lượt đủ dài. Lệch một chút so với gạch đầu dòng "không còn assertion
nào dựa vào spokenLines" — bỏ hẳn thì không có gì chặn TTS lặng lẽ quay lại.

Test `nói 'ừ' để xác nhận bằng giọng nói` đã có sẵn và vẫn xanh: đường vào bằng
giọng nói còn nguyên vẹn.

Ảnh chụp cũng cho thấy sẵn việc của C-024: khoảng trống mênh mông giữa bảng và
ô chữ.
