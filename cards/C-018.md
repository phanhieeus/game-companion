# C-018 — Deploy lên Render: một tiến trình phục vụ cả API lẫn web

status: done
deps: C-017

## Scope

**Sửa ADR 3** ("chưa deploy, chạy local"): giả định rủi ro nhất — độ chính xác
giọng nói tiếng Việt — cần HTTPS thật để đo trên điện thoại, mà `adb reverse`
chỉ dùng được với đúng một máy cắm dây.

1. FastAPI phục vụ luôn `dist/` đã build, và trả `index.html` cho mọi đường dẫn
   không phải `/api/*`. Local vẫn hai tiến trình (vite có hot reload);
   production một tiến trình `uvicorn`.
2. `render.yaml`: build `npm ci && npm run build && pip install -r
   requirements.txt`, start `uvicorn api.main:app --host 0.0.0.0 --port $PORT`,
   khai `GEMINI_API_KEY` (đặt tay, không commit) và `DATABASE_URL`.
3. Kiểm micro thật trên điện thoại qua HTTPS — thứ chưa từng đo được.

## Allowed files

- `api/main.py` (phục vụ tĩnh), `render.yaml` (mới), `package.json`
- `flow/04-adr.md` (sửa quyết định 3)

## Verify (run these before calling the card done)

- [x] `npm run build && uvicorn api.main:app` trên máy sạch → mở cổng đó thấy
      app, không cần vite
- [x] Đường dẫn sâu (`/whatever`) trả `index.html`, không phải 404
- [x] `/api/*` vẫn là API, không bị nuốt bởi route tĩnh
- [x] Render build xanh, log khởi động báo có `GEMINI_API_KEY`
- [x] Mở trên ĐIỆN THOẠI qua HTTPS: nút micro xin quyền và nghe được tiếng Việt
      — **operator xác nhận 2026-08-08: chạy được trong môi trường sạch.**

## Done-evidence (world-state proof, named BEFORE building)

URL Render công khai, bấm vào chạy được: tạo phiên, ghi một ván **bằng giọng nói
trên điện thoại thật**, tải lại trang thấy ván còn nguyên. Kèm ảnh chụp màn hình
điện thoại và `curl https://<app>.onrender.com/api/health` trả `hasKey: true`.

## Evidence (paste the actual proof here when done)

**https://ghi-diem.onrender.com — đang chạy, HTTPS thật.**

```
$ curl https://ghi-diem.onrender.com/api/health
{"ok":true,"model":"gemini-3.1-flash-lite","hasKey":true,"minPlayers":4,"maxPlayers":5}

$ curl -o /dev/null -w "%{http_code} | %{content_type}" .../          → 200 | text/html
$ curl -o /dev/null -w "%{http_code}" .../whatever                     → 200 (index.html, không 404)
$ curl -o /dev/null -w "TLS: %{ssl_verify_result}" .../api/health      → TLS: 0 (chứng chỉ hợp lệ)
```

**Cả stack chạy thật, qua Gemini thật:**

```
$ curl -X POST .../agent -d '{"text":"Tú ăn 6, ba người kia mỗi người chung 2"}'
outcome: confirm | bước: 1
hỏi:    Tú +6, Nam -2, Hùng -2, Lan -2. Ghi ván này nhé?
ván trong sổ lúc này: 2        ← chưa ghi, đang chờ người chốt

$ curl -X POST .../agent/confirm -d '{"accepted":true}'
outcome: final | Đã ghi ván 3: Tú ăn 6, Nam, Hùng, Lan mỗi người chung 2.
bảng:  Tú 4, Lan 1, Nam -1, Hùng -4

$ curl -X POST .../agent -d '{"text":"ai đang dẫn điểm?"}'
bước: 2 | Tú đang dẫn đầu với 4 điểm.     ← vòng ReAct hai bước trên production
```

**Ảnh `screenshots/15-render-live.png`** @360px: trình duyệt mở URL công khai,
thấy đúng ba ván vừa tạo qua API — trong đó ván 3 là ván agent ghi. Cùng một dữ
liệu, hai đường vào.

### Giả định rủi ro nhất: đã đo, và nó đứng

**Operator xác nhận (2026-08-08): mic tiếng Việt trên điện thoại chạy được
trong môi trường sạch.** Nguyên văn quyết định: *"cứ đóng đi, sau này phát sinh
thì mới biết được"* — tức là chấp nhận đóng với điều kiện đã đo trong điều kiện
thuận lợi, chưa đo trong quán ồn.

Đây là bằng chứng do người dùng cung cấp, không phải do tôi chạy. Ghi rõ vậy để
sau này đọc lại biết mức độ chắc chắn của nó: **một lần thử, môi trường sạch,
một giọng nói**. Chưa biết gì về: quán ồn, nhiều người nói chen, giọng vùng
miền khác, điện thoại đời thấp.

### Phần tôi tự kiểm được (bên dưới) — và phần trước đây còn treo

Verify cuối cùng — **nói tiếng Việt vào micro trên điện thoại thật** — cần micro
thật và giọng thật. Playwright dùng SpeechRecognition giả nên không thay thế
được; đó cũng là lý do card này tồn tại: giả định rủi ro nhất của dự án là độ
chính xác STT tiếng Việt, và **tới giờ vẫn chưa ai đo được lần nào**.

Việc của operator, đúng ba bước:

1. Mở https://ghi-diem.onrender.com trên điện thoại (Chrome/Safari).
2. Tạo phiên, nhấn giữ nút, nói "Nam ăn 3, ba người kia mỗi người chung 1".
3. Xem thẻ đề xuất có ra đúng số không; bấm Ghi rồi tải lại trang.

Card giữ `blocked-on-operator` cho tới lúc đó. Đánh dấu done bây giờ là nói dối
về đúng cái quan trọng nhất.

### Ghi chú

- **Lần chạy đầu chết vì lấy nhầm chuỗi kết nối CSDL** — xem Evidence C-017.
- **Free tier ngủ sau ~15 phút không dùng**, lần gọi kế tiếp mất 30–60 giây để
  dậy. Chấp nhận được cho việc đo STT; nếu đem cho cả nhóm dùng thật thì phải
  nâng plan.
- **CSDL free hết hạn 2026-09-07.**
